name: flutter_map_marker_cluster
description:
  A Dart implementation of Leaflet.makercluster for Flutter apps.
  Provides beautiful animated marker clustering functionality for flutter_map.
version: 0.2.7

homepage: https://github.com/lpongetti/flutter_map_marker_cluster

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_map: #^0.8.2
    git:
      url: git://github.com/bugDim88/flutter_map.git
      ref: WMS_support
      
  latlong: ^0.6.1

dev_dependencies:
  flutter_test:
    sdk: flutter
